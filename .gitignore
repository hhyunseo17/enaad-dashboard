# 데이터 원본 (민감 — 커밋 금지)
addata.xlsx
*.xlsx

# 원본 백업 압축본 (data_backup.zip 등). 안에 xlsx가 들어 있으므로 *.xlsx 규칙만으로는
# 걸러지지 않는다 — 확장자가 zip이라 그대로 커밋될 수 있어 별도로 차단한다.
*.zip

# OS/에디터
.DS_Store
Thumbs.db
.vscode/
.idea/

# 로그
*.log

# ETL (scripts/etl) — 자격증명·의존성
scripts/etl/.env
scripts/etl/node_modules/

# 로컬 wrangler pages dev용 시크릿(SUPABASE_SERVICE_ROLE_KEY 등) — 커밋 금지
.dev.vars
.wrangler/
